# Swiggy Clone Project

A clone of Swiggy (food delivery) made with frontend + backend.

---

## 📁 Structure

- `Frontend/` — the UI of the app (HTML, CSS, JS , react)
- `Backend/` — server-side logic (django,APIs, database etc)
